{
  "name": "stencil-component-workshop",
  "version": "1.0.0",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "bootstrap": "lerna bootstrap",
    "build": "lerna run build",
    "gh-pages": "gh-pages-deploy"
  },
  "gh-pages-deploy": {
    "staticpath": "packages/presetnation/dist"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "gh-pages-deploy": "^0.5.0",
    "lerna": "^2.10.1"
  }
}
